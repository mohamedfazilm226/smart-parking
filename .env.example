# Copy to .env and fill in. Never commit .env.
# From project: oss_host is your API base URL.
# Anon (public) key: run `insforge metadata --json` or open InsForge dashboard → API keys.

VITE_INSFORGE_URL=https://YOUR_APPKEY.us-east.insforge.app
VITE_INSFORGE_ANON_KEY=your_anon_key_here
